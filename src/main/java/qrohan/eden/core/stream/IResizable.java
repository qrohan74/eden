package qrohan.eden.core.stream;

/**
 * Interface for resizable objects
 */
public interface IResizable {
	/**
	 * Update object dimension
	 * @param newDimension new dimension
	 */
	void updateDimension(long newDimension);
}
